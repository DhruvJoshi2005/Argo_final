# Connects Grafana to Prometheus on startup (no manual UI setup).

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: argo-prometheus          # referenced by the dashboard panels
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
